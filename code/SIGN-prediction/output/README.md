# Prediction outputs

Each run writes an independent directory containing `predictions.npz`,
`metrics.csv`, `config.json`, training history, the trained joint decoder, and
Phase-I provenance. Generated run directories are ignored by `.gitignore`.
