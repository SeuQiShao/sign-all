# Code

- `plot_fig3.py`: self-contained Fig. 3 renderer. It reads only `../plot_data` and writes all exports to `output`.
- `export_plot_data.py`: optional exporter that converts experiment inputs into the compact CSV files used by the renderer.

The plotting backend is Python/matplotlib. Run the renderer with the Python
environment containing the listed dependencies:

```text
python
```
