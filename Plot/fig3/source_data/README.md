# Raw source data policy

No raw source observations are included in this submission package. The Fig. 3 renderer is intentionally self-contained and reads the compact, publication-ready CSV tables in `../plot_data` directly.

`../code/export_plot_data.py` documents an optional route for generating the
compact tables from the corresponding experiment inputs.
