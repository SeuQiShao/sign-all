# Source data

This directory is intentionally empty of raw source observations. Fig. 2 is
reproducible from the compact, plot-ready CSV matrices in `../plot_data`, so
the raw XLSX coefficient tables and the original workspace paths are not
required for the final figure package.

If raw observations are later required for an audit, place them here without
changing the plotting code; the current script remains driven by
`../plot_data`.
