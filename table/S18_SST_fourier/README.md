# Supplementary Table S18 source data

This directory stores the retained nonzero Fourier components and fitted
coefficients reported in the SST model. `coefficients.csv` is a wide CSV with
paired columns for the sine and cosine components. It contains 28 nonzero
sine coefficients and 29 nonzero cosine coefficients; the final cosine row
has an empty sine entry.

